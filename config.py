# config.py

# Enable Flask's debugging features. Should be False in production
DEBUG = True

#note this is very simplified, need better for more complex application
#ie we'll have to change this
#have diff config.py files for testing, dev, prod, and put it into
#a big config directory